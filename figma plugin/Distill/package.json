{
  "name": "distill",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild code.ts --bundle --target=es2017 --format=iife --outfile=code.js",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.130.0",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3"
  }
}
